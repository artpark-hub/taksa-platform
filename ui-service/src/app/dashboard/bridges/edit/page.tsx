import { Suspense } from 'react';
import EditBridge from '../../../../components/edit-bridge/EditBridge';

export default function EditBridgePage() {
    return (
        <Suspense>
            <EditBridge />
        </Suspense>
    );
}
