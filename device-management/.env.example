# Taksa Platform DM Configuration
# Service: Device Management (DM)
# Follows taksa-deployments naming convention (TAKSA_DM_* prefix)
# Non-action and action settings: defaults in configs/config.yaml; TAKSA_DM_* env vars override when set.

# Data persistence directory (use absolute path or relative to docker-compose.yml)
TAKSA_DM_DATA_DIR=./data

# API Server Configuration
TAKSA_DM_HTTP_PORT=8000
TAKSA_DM_GRPC_PORT=9000

# Logging Configuration
TAKSA_DM_LOG_LEVEL=debug
TAKSA_DM_LOG_FILE=/data/taksa-device-management.log

# For PostgreSQL (use TAKSA_PG_* from taksa-deployments):
TAKSA_DM_DATABASE_DRIVER=postgres
TAKSA_DM_DATABASE_SOURCE=postgres://user:password@localhost:5432/taksa_platform_dm?sslmode=disable

# Deployment Configuration (for device registration Docker command generation)
TAKSA_DM_BASE_URL=http://localhost:8000
TAKSA_DM_UMH_CORE_DOCKER_IMAGE=management.umh.app/oci/united-manufacturing-hub/umh-core:v0.43.17

# Edge status auto re-subscribe on pull when catalog/status is stale (default true). Set false for heavy DCD fleets.
# TAKSA_DM_AUTO_RESUBSCRIBE_STATUS_MESSAGES=false

# NATS cluster URLs (comma-separated) for auto-deployed UNS→NATS mirror on DCD login. Leave empty to disable.
# Example: nats://user:pass@nats.example.com:4222
TAKSA_DM_NATS_MIRROR_URLS=

# Action lifecycle — defaults in configs/config.yaml (action_cleanup.*); env overrides when set.
# Retain terminal actions (COMPLETED, FAILED, EXPIRED, CANCELLED) and related messages for this many minutes.
# config: action_cleanup.retention_minutes (default 60). <= 0 clamps to default with a warning.
TAKSA_DM_ACTION_RETENTION_MINUTES=60

# How often the cleanup/expiry sweep runs (minutes).
# config: action_cleanup.cleanup_interval_minutes (default 10). <= 0 disables the loop.
TAKSA_DM_ACTION_CLEANUP_INTERVAL_MINUTES=10

# Auto-expire QUEUED UI/async actions after this many minutes (0 = off).
# config: action_cleanup.auto_expire_minutes. Excludes subscribe and UNS-to-NATS-mirror deploy/edit.
# TAKSA_DM_ACTION_AUTO_EXPIRE_MINUTES=30
